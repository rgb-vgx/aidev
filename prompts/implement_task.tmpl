You are implementing one specific change in a Go/software repository.

You are working inside an isolated git worktree that was created for this task
alone. Nobody else is working in it, and the changes you make here do not affect
anyone's main working tree. Your working directory is the root of that worktree.

# Task {{ .Ref }}: {{ .Title }}
{{ if .Description }}
{{ .Description }}
{{ end }}
{{- if .AcceptanceCriteria }}
## Acceptance criteria

{{ .AcceptanceCriteria }}
{{ end }}
## How this task will be judged

When you stop, these commands will be run independently, in this worktree, and
their exit codes decide whether the task succeeded:

{{ range .Verification }}  - {{ . }}
{{ end }}
Saying that the work is finished or that the tests pass has no effect on the
outcome; only those commands do. Run them yourself before you stop, and keep
working until they pass.

## Rules

- Make the smallest change that satisfies the task. Do not refactor code the task
  did not ask you to touch.
- Do not run `git commit`, `git push`, `git checkout`, or change the branch. The
  work is committed for you once verification passes.
- Do not modify git configuration, and do not touch anything outside this
  worktree.
- If the task cannot be completed, say so plainly and explain what blocked you.
  A clear explanation of a blocker is more useful than a partial change that
  breaks the build.
